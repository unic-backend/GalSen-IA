# =============================================================================
# GalSen IA — Optional Dependencies
# =============================================================================
# Install with: pip install -r requirements-optional.txt
#
# Ces dépendances sont importées en lazy dans le code.
# Leur absence désactive les fonctionnalités correspondantes
# mais ne casse pas le reste de la plateforme.

# === Document Processing ===
# Document Intelligence Engine: PDF, DOCX, XLSX, PPTX, MD loaders
PyPDF2>=3.0.0
python-docx>=1.0.0
openpyxl>=3.1.0
python-pptx>=1.0.0
markdown>=3.4.0

# === OCR ===
# OCR Tool (nécessite aussi Tesseract installé sur le système)
pytesseract>=0.3.10

# === Embeddings ===
# Embeddings Tool (modèles lourds ~500 Mo+)
sentence-transformers>=3.0.0

# === Rendu HTML/CSS/SVG en trames ===
# Sonde `browser_render` (`src/media/core/capabilities.py`). Importé uniquement
# dans la sonde, dans un `try/except ImportError` dont l'échec est le résultat
# mesuré : absent, la capacité est DEGRADE et le moteur refuse le travail au
# lieu de le simuler. Poids : le paquet est léger, les navigateurs qu'il faut
# ensuite installer (`playwright install chromium`) pèsent ~150 Mo.
playwright>=1.40.0

# === Advanced ML ===
# Knowledge Engine (ranking TF-IDF)
scipy>=1.10.0

# === AI Providers ===
# Model Engine providers (chaque provider est optionnel)
# OpenAI
openai>=1.0.0
# Anthropic
anthropic>=0.30.0
# Google Generative AI
google-generativeai>=0.3.0