# GalSen IA — Completed Work

Journal en ajout seul. **Ne pas le lire en entier** — le chercher.
Entrées antérieures au 2026-08-19 → `docs/memory/archive/completed-work-2026.md`.

### 2026-08-19 (MASTER UPDATE DIRECTIVE V4 — MoneyPrinterTurbo intégré comme fournisseur déclaré : 15 phases sur 15)
- **La phrase du programme : MoneyPrinterTurbo ne génère pas de vidéo.** Vérifié dans sa source et non dans son README — `material.py` télécharge des rushes de Pexels et Pixabay, `video.py` les assemble avec moviepy en déléguant à `ffmpeg`. Aucun modèle ne produit un pixel. **L'avoir su avant d'écrire l'adaptateur est toute la valeur des 19 étapes d'audit** que §36 impose : écrit en premier, il aurait été déclaré `text_to_video`, et un routeur aurait fini par servir les rushes d'un inconnu à qui demandait son ami.
- **La décision la plus lourde : une tâche nouvelle, `stock_assembly`**, déclarée dans les deux vocabulaires (média et créatif). Deux actes, deux noms. ADR-030.
- **Premier fournisseur jamais sélectionné par le routeur créatif** des deux programmes : `stock_assembly` non commercial → `SELECTED` ; commercial → `NO_PROVIDER` (droits sur la sortie non lus) ; `text_to_video` → jamais offert.
- **Le dépôt est MIT, la capacité voulue est copyleft.** `edge-tts` — le TTS, c'est-à-dire la seule vraie raison d'intégrer MPT — est **LGPL-3.0**, et le SDK Azure est propriétaire. C'est exactement la confusion que §30 existe pour empêcher. Invocation par **API et jamais par import**, ce qui est une décision juridique autant qu'un choix d'empaquetage, et **aucune dépendance ajoutée**.
- **Trois abstractions de fournisseurs trouvées, pas deux** : `model_engine/providers` sélectionne des modèles de langue et n'a de commun que le mot. La trouver **en auditant** est précisément l'intérêt d'auditer avant d'écrire.
- **Quatre des six étapes média bloquées le sont par un `ffmpeg` manquant, pas par un fournisseur.** Un générateur vidéo n'en débloque au mieux que 2 sur 17 — mesure qui recadre tout le programme.
- **§21 : 8 KEEP, 3 EXTEND, 1 ADAPT, 0 DEPRECATE, 0 REPLACE.** Aucune preuve trouvée pour un quelconque remplacement, et « un fournisseur plus récent existe » n'en est pas une.
- **Deux défauts à moi trouvés en écrivant les tests** : `min_vram_gb = 0` était traité comme un besoin de VRAM et écartait un fournisseur qui n'a besoin d'aucun GPU ; et l'adaptateur déclarait 1920×1080, si bien qu'un 1080×1920 vertical — **le cas d'usage principal** — était refusé sur sa hauteur.
- **Trois gardes du dépôt en ont attrapé trois autres**, tous à raison : le chargeur du corpus (une valeur sans dire comment elle a été établie), le compte d'ADR (30 → 31), et trois variables d'environnement introduites sans documentation.
- **19 des 22 tests d'or de §38 étaient déjà couverts** ; 3 écrits. Les cartographier d'abord a évité de gonfler le compte sans ajouter de couverture.
- Reste dû, nommé : une décision TTS à part entière (`kokoro-tts` est MIT et local), `whisperx` pour la séparation de locuteurs, l'écart `None` entre couches, et la lecture des conditions Pexels/Pixabay.
- Vérifié : `python -m pytest -q` → **6 233 passent, 11 ignorés, 1 échec** (`v0.1.0`, antérieur). 39 tests ajoutés. `ruff` propre.

### 2026-08-19 (CREATIVE CANVAS & CINEMA ORCHESTRATION — 17 phases sur 17, ADR-031)
- **La phrase du programme : il n'y avait rien à importer.** §4 rend **0 KEEP, 0 ADAPT, 3 REFERENCE ONLY, 2 REJECT** — quatre des cinq dépôts sont des applications JavaScript ou Electron face à une plateforme Python, et **deux n'ont aucune licence** sur quatre noms de fichier × deux branches. La directive les annonce MIT tous les deux : c'est faux, mesuré. Le nombre de dépendances ajoutées par ce programme est **zéro**.
- **Le risque attendu était d'importer trop ; le risque mesuré était de reconstruire l'existant.** K00 a compté neuf des onze sous-systèmes de §5–§17 déjà en place, trois registres, deux systèmes de provenance. Un seul type manquait vraiment : `ProviderPrivacyPolicy`.
- **Les deux dépôts sans licence servent un `index.html` et un `src/main.js` identiques au octet près** (SHA-256). L'un repackage l'autre : « laquelle des deux implémentations est supérieure » n'a pas de réponse pour cette paire.
- **Une règle de sécurité écrite dans ce programme a été cassée par sa propre vérification.** La confiance d'un nœud de génération dérivait de `CreativeProvider.invocation` — champ que `adapt_declared()` calcule **depuis la licence du dépôt**. Un fournisseur joint par HTTP chez un tiers y ressort `IN_PROCESS` et sa sortie aurait porté `TOOL`. Corrigé avant d'être du code : la confiance vient de `data_destination`, dont l'`UNKNOWN` retombe sur `EXTERNAL`.
- **Deuxième fois que deux couches lisent un même champ à l'envers** (après `min_vram_gb = None`). Deux occurrences font un motif : ADR-031 acte que le prochain changement touchant l'un des deux le résout.
- **`CreativeIntent` a quatre statuts, pas trois.** `NOT_REQUESTED` n'est pas déclarable et n'est pas `OPTIONAL` : le confondre est ce qui autorise un préréglage à poser un panoramique que personne n'a demandé. `offer()` propose, `accept()` est un acte séparé.
- **La couche cinéma refuse de calculer ce qu'elle ne peut pas** : champ de vision `UNKNOWN` sans largeur de capteur déclarée (un nom de format ne porte pas de dimension), et **aucune profondeur de champ chiffrée**, jamais, le cercle de confusion dépendant d'un support que rien ne déclare.
- **ADR-031 décidait cinq modules et le plan ne leur allouait aucune phase** — même faute que le §46/StyleEngine du programme précédent. K07 les livre et fait passer la tranche verticale à travers : `intent OK, graph OK, shot OK, privacy BLOCKED, handover OK, readiness BLOCKED`, `produced_artifact: None`.
- **Le garde de lint du dépôt tourne `ruff` sur *tout* le dépôt**, pas seulement `src` et `tests` — il a attrapé un import inutilisé à moi que `ruff check src tests` laissait passer.
- Mesuré : ordre topologique **0,003 ms**, `check_plan` **0,014 ms**, mais `graph_readiness` **23,8 ms** — dont **24,8 ms** de sonde média seule. Le coût est la sonde, pas le graphe ; le mettre en cache irait vite et mentirait au premier `ffmpeg` installé.
- Reste dû, nommé : renseigner une vraie `ProviderPrivacyPolicy` (installer un fournisseur local et observer ses sockets), l'écart `None`/`invocation` entre couches, `ffmpeg`, et `git push origin v0.1.0`.
- Vérifié : `python -m pytest -q` → **6 363 passent, 11 ignorés, 1 échec** (`v0.1.0`, antérieur aux trois programmes). 130 tests ajoutés. `ruff check .` propre. Rapport → `docs/canvas/final-report.md`.

### 2026-08-19 (RESEARCH ORCHESTRATION INTEGRATION — 18 phases sur 18, ADR-032)
- **La phrase du programme : un seul des deux candidats est une bibliothèque, et aucun ne peut tourner ici.** `Agent-Reach` 1.5.0 exporte une seule classe publique, dont les méthodes sont `doctor()` et `doctor_report()` ; toute la capacité vit dans un `cli.py` de **87 606 octets avec 43 appels à `subprocess`**, qui route quinze plateformes vers des CLI tiers. Écrire l'adaptateur d'abord aurait donné à la plateforme huit programmes externes non versionnés, une session Chrome de bureau qu'elle ne peut pas avoir, et une discipline exactement inverse de `acquisition/fetcher.py`.
- **Trois des six programmes qu'Agent-Reach orchestre n'ont aucun fichier de licence** (`twitter-cli`, `rdt-cli`, `bilibili-cli`) : leurs README répondent `200`, donc ils existent et sont **non licenciés**, pas injoignables. Ce sont son backend Twitter par défaut, son repli Reddit et son backend Bilibili préféré.
- **Le seul copyleft des 19 dépendances est `browser-cookie3`** — le paquet qui lit les cookies de session dans le profil navigateur. Extra optionnel, version LGPL `UNKNOWN` (métadonnée « lgpl » minuscule, sans numéro).
- **Vérifié plutôt que supposé** : le `LICENSE` de `jina-ai/reader` s'ouvre sur « All rights reserved » — c'est un en-tête de copyright **au-dessus** du texte Apache-2.0.
- **La décision de sécurité du programme : où vit le contrôle d'adresse.** Trois implémentations existaient, aucune ne dominait. `src/research/safety.py` le tient désormais **une fois**, bloquant les plages internes **en littéral et en résolu** — ce qui ferme le trou que `web-search-mcp` nomme dans sa propre docstring. Ce qu'il ne prétend pas : la fenêtre de re-résolution DNS **reste ouverte**, et `not_guaranteed` le dit.
- **Le champ s'appelle `execution` et non `invocation`** : ADR-031 avait enregistré que ce mot porte déjà deux sens opposés ; un troisième aurait été pire.
- **Une régression introduite et corrigée** : `tests/research/test_providers.py` partageait son basename avec `tests/media/test_providers.py`, et sans `__init__.py` sous `tests/`, pytest **interrompait toute la collecte** — aucun test ne tournait. Le fichier passait seul, ce qui est précisément pourquoi la règle permanente exige la suite complète.
- **Cinq des huit mesures de STEP 13 rendent `NOT_MEASURED` avec leur raison** : rien n'exécute, donc un chiffre dans ces colonnes serait inventé. Mesuré en revanche : `route` **0,164 ms**, pipeline complet sans recherche **0,163 ms**, `normalize` **0,016 ms**, `check_url` **0,007 ms**. Le taux de cache est **déclaré synthétique** là où il est rendu.
- **12 capacités déclarées, 3 routables** (`web_search`, `page_fetch`, `github_read`), toutes servies par les outils de la plateforme. Une demande portant une donnée personnelle est **refusée pour tous les fournisseurs**, faute de destination lue.
- **Neuf composants réutilisés, aucun réécrit** : `LicenceRecord`, `ProviderPrivacyPolicy`, `security/trust`, l'échelle d'observation, `fingerprint`, le format d'acquisition, `CreativeCache` en entier, le vocabulaire de `mvp.py`, et la limitation de débit existante.
- Reste dû, nommé : installer `web_search_mcp` et mesurer (cinq colonnes deviendraient des chiffres, et le cas 13 testable), lire les conditions d'un service, l'écart `min_vram_gb`/`invocation` d'ADR-031, et `git push origin v0.1.0`.
- Vérifié : 219 tests dans `tests/research`, `ruff check .` propre. Rapport → `docs/research/final-report.md`.
### 2026-08-19 (LIVE CONTEXT ENGINE / CALL.MD — 27 phases sur 27, ADR-033)

- 2026-08-19 — **Live Context Engine terminé** (`src/live_context/`, ADR-033, 27 phases, 16 volets). Rapport → `docs/live-context/final-report.md`. 16 modules, 376 tests, **aucune dépendance ajoutée**.
- **L'état est calculé, jamais écrit** : `readiness()` rend `REPRESENTATION READY — NO LIVE PERCEPTION ON THIS MACHINE, 5 STAGE(S) NOT IMPLEMENTED, 2 BLOCKED` — 9 `READY`, 2 `BLOCKED`, 5 `ABSENT`. La coupure est totale : **toutes** les étapes de représentation tournent, **aucune** étape de perception. Un test remplace la liste d'étapes et vérifie que le verdict change.
- **Les quatre audits ont réduit le programme, pas l'inverse.** Call.md n'enregistre pas sous Linux (sa propre table le dit) ; VideoDB y porte la capture, la transcription *et* l'inférence ; six des neuf items « ne pas dupliquer » du §41 existaient déjà ; et la licence n'est pas l'obstacle (48 MIT sur 54 paquets, l'arbre le plus propre des cinq dépôts audités en quatre programmes) — c'est l'architecture, la plateforme et la souveraineté.
- **`ABSENT` n'est pas `UNKNOWN`**, et c'est la distinction la plus coûteuse du volet : l'un est mesuré et ne changera pas, l'autre attend une mesure. Une absence sans constat est refusée à la construction.
- **Le consentement est nécessaire, jamais suffisant** : les refus inconditionnels d'ADR-018 sont évalués **avant** lui, et un test le prouve avec la dérogation `screen_capture` posée. Aucune ADR n'est amendée par ce programme.
- **Aucun locuteur numéroté, aucun zéro là où rien n'a été compté** (`turns`, `switch_count`, `speaker_count`, les six latences du §33 : `None`). Aucune prétention au temps réel : `realtime_claim()` répond ni oui ni non.
- **Le `NudgeEngine` du §20 n'a pas été écrit** : c'est `src/proactive/`, dont la suppression par empreinte des preuves est plus précise que le minuteur de deux minutes de Call.md. Quatre des cinq interfaces du §31 non plus.
- **30 scénarios exécutables** (`golden.py`) — 24 `VERIFIED`, 6 `BLOCKED`, 0 échec. Vérifié : 376 tests dans `tests/live_context`, suite complète **6 958 passent, 12 ignorés, 1 échec** (`v0.1.0`), `ruff check .` propre.
### 2026-08-20 (OPENCLAW — audit, 19 phases sur 19, ADR-034 : ne pas intégrer)

- 2026-08-23 - `/chat` rédige (`src/chat/`, ADR-039) - 19 phases. Couche de réponse branchée sur `ModelManagerImpl`, ancrage calculé avant génération.
- 2026-08-23 - Décision : écrire n'ancre jamais, et `ChatResponse.generated` le prouve - un refus composé doit rester distinguable d'une réponse - si c'est faux, la plateforme ment exactement là où elle prétend ne pas mentir.
- 2026-08-23 - Décision : rétablir les trois cas de `selection_appliquee()` au lieu d'affaiblir la garde du pipeline - `recommended_agents()` faisait déjà la distinction dans sa docstring - si c'est faux, une heuristique muette n'exécute plus rien du tout.
- 2026-08-23 - Décision : ne plus rendre la prose du fournisseur dans `generation_unavailable` - elle livrait `http://localhost:11434` à tout appelant - si c'est faux, l'exploitant perd le message actionnable, qui reste au journal et dans `/health`.
- 2026-08-23 - Constat non corrigé : `coder` n'est pas déclaré dans le workflow `question`, donc une demande de code ne l'atteint pas. Décision d'exploitant (§19), épinglée par un test.
- 2026-08-23 - Constat non corrigé : sur une question sénégalaise à base vide, le verdict de `senegal` prime sur un constat vérifié du chercheur (`_ancrage_de`). Épinglé par un test.
- 2026-08-23 - Audit architecture noyau Linux terminé et fusionné (PR #36) - `docs/research/linux-kernel-architecture-audit.md`, verdict SELECTIVE ARCHITECTURAL IMPROVEMENTS RECOMMENDED, aucune recommandation autorisée.
- 2026-08-23 - Redesign chat-first (`src/api/server.py` `POST /chat`, `src/web/static/`) - 8 chapitres, 11 phases. `/ui/` sert la conversation, `/ui/admin/` le tableau de bord.
- 2026-08-23 - Décision : `/chat` passe par l'orchestrateur, pas par `model.generate` - la détection de domaine existe déjà dans `src/router/` - si c'est faux, chaque tour paie 1,1 s d'un pipeline d'agents pour une question qui n'en avait pas besoin.
- 2026-08-23 - Décision : rendre le refus des agents plutôt que de générer pour combler - l'agent `senegal` dit déjà « la base est vide, ce n'est pas une réponse négative » - si c'est faux, le chat est honnête mais ne converse pas, et l'utilisateur le voit dès la première bulle.
- 2026-08-23 - Constat : aucun des 17 agents ne rédige (`agents/`) - seuls `planner` et `coder` appellent le modèle. Une étape de rédaction reste à décider, non autorisée.
- 2026-08-23 - Défaut corrigé : le jeton d'ancrage fabriquait sa classe par interpolation et n'a jamais eu sa couleur - `tests/test_ui_chat.py::TestJetonAncrage` le tient, sabotage vérifiée.
- 2026-08-20 — **Audit OpenClaw terminé, décision : ne pas intégrer** (ADR-034, 19 phases, 13 volets). Rapport → `docs/openclaw/feasibility-gates.md`. **Zéro ligne de `src/` modifiée, zéro dépendance, zéro test touché.**
- **Trois des douze portes du §19 répondent `NON`** : le bac à sable (désactivé par défaut, passerelle non isolée, `tools.elevated` sur l'hôte — *« This is not a perfect security boundary »*, leurs mots) ; l'isolation multi-utilisateurs (*« Session IDs select routing; they do not authorize one tenant against another »*) ; et la complexité (treize des quatorze capacités du §5 existent déjà ici).
- **La couche d'isolation qu'exige le §8 est impossible ici** : elle demande des espaces de noms et des cgroups que `src/sandbox/policy.py` consigne déjà comme absents. Le blocage précède ce programme.
- **Le vrai manque est un canal, pas un runtime** : canaux conversationnels bidirectionnels, contre trois canaux de notification à sens unique. `src/connectors/` a déjà le contrat (liaison au sujet obligatoire et vérifiée à l'enregistrement). Chiffrer un connecteur WhatsApp est **recommandé comme programme séparé**, non autorisé par celui-ci.
- **À retenir au-delà d'OpenClaw** : ADR-014 met le mode souverain à vrai et n'enregistre pas les fournisseurs hébergés ; **tout runtime subordonné portant son propre magasin d'identifiants est un trou que le test actuel ne peut pas voir**, puisqu'il éprouve le chemin de modèle de GalSen IA.
- **Cinq constats sur GalSen IA elle-même**, consignés pour `pending-work`, aucun corrigé : `ApprovalRequest` sans sujet ; état de révocation en mémoire de processus ; `load_capabilities()` sans cache à ~22 ms (latent) ; deux vocabulaires de jobs et deux `retry_manager` ; et une suggestion optionnelle non implémentée (pseudonymes à clé d'installation).
- Vérifié : suite complète **6 958 passent, 12 ignorés, 1 échec** (`v0.1.0`), `ruff check .` propre, une régression complète par phase.
### 2026-08-20 (DEEPSEEK HARNESS — audit, 14 phases sur 14, ADR-035 : option C, pas encore)

- 2026-08-20 — **Audit DeepSeek Harness terminé, décision : OPTION C** (ADR-035, 14 phases, 11 volets). Rapport → `docs/deepseek-harness/final-report.md`. **Zéro ligne de `src/`, zéro dépendance, zéro test ajouté ou modifié.**
- **Quatrième back-end de codage**, à côté d'`aider`, `openhands`, `swe_agent` — pas l'orchestrateur. **Implémentation non autorisée** : trois conditions nommées, dont *mesurer la qualité*.
- **Aucune porte n'échoue franchement** (7 `OUI`, 3 `UNKNOWN`, 1 scindée), là où le sujet précédent en manquait trois sur douze. Motif : **tout ce qui est structurel passe, tout ce qui est empirique est `UNKNOWN`** — le structurel passant sur du travail déjà fait (routeur par capacités d'ADR-028, `degradation.py`, `check_availability`).
- **Le bac à sable de DSH ne peut pas tourner sur cet hôte**, mesuré trois fois : `bwrap` absent, `landlock_create_ruleset` → `ENOSYS`, stub faible dans `kallsyms`, aucun LSM. La complémentarité trouvée en D04 est donc **architecturale, pas déployable ici**.
- **`BENCHMARK.md` ne publie aucun score** : le dépôt ne contient aucune preuve comparative de qualité de codage. 170,4 k étoiles sur un `0.1.0-rc` mesurent l'attention.
- **Premier sujet en cinq programmes dont l'arbre de dépendances a pu être lu** : ~130 paquets, ~98 % permissifs, deux copyleft cantonnées au développement *par le projet lui-même*, une `SEE LICENSE IN` non lue.
- **Deux alignements réels** : l'approbation **échoue fermée** des deux côtés (`never` = rejet déterministe), et DSH refuse le *« silent unconfined passthrough »* comme ce dépôt refuse les réponses plausibles d'une capacité inachevée.
- **Trois constats sur GalSen IA**, consignés, aucun corrigé : **aucun fichier `LICENSE`** ici ; le test de souveraineté ne couvre pas les runtimes subordonnés (2ᵉ occurrence) ; `load_capabilities()` sans cache à ~22 ms.
- Vérifié : suite complète **6 958 passent, 12 ignorés, 1 échec** (`v0.1.0`), une régression par phase, `ruff check .` propre.

### 2026-08-20 (PR #31 fusionnée — les trois programmes entrent sur `main`)
- **`1e09f6d`** : Live Context (ADR-033), audit OpenClaw (ADR-034), audit DeepSeek Harness (ADR-035) — 42 commits, 72 fichiers.
- **CI vérifiée avant fusion, pas supposée** : `1 failed, 6955 passed, 15 skipped`. L'unique échec reste l'étiquette `v0.1.0`, identique sur `main`.
- **L'étiquette n'a pas pu être poussée d'ici** : `git push origin v0.1.0` → **`HTTP 403`**, mesuré. La passerelle git de l'environnement laisse passer la branche désignée et refuse les étiquettes. La cible correcte est `383fcf7` (`chore(release): prepare v0.1.0`, 2026-08-11), qui correspond au `## [0.1.0] - 2026-08-11` du changelog. **Pousser l'étiquette publie une release GitHub** (`release.yml` sur `v*`) : c'est la décision du propriétaire.
- **Trois mensonges de la mémoire corrigés** : `current-objectives.md` et `priorities.md` annonçaient C6 comme seul critère tenu (la feuille de route en compte **quatre** : C2, C3, C5, C6), gardaient ouverte la question des utilisateurs **tranchée par ADR-029 le 2026-08-18**, et disaient la base de connaissances **vide** alors qu'elle porte **212 objets secteur sur 4 domaines, 14 régions, 45 départements** — mesurés sur disque, et **10 domaines portant la raison de leur vide**.
- **`completed-work.md` ramené de 1 170 à ~70 lignes** : tout ce qui précède le 2026-08-19 versé dans `archive/completed-work-2026.md` (deuxième versement). Rien supprimé. Trois programmes qui vivaient en puces sous le titre du précédent ont reçu le leur.

### 2026-08-20 (Souveraineté des runtimes subordonnés — le trou relevé deux fois est fermé)
- `tests/test_sovereignty_subordinate_runtimes.py`, **9 tests, aucune ligne de `src/` modifiée**. ADR-034 et ADR-035 avaient relevé le même angle mort sur deux projets différents : le test de souveraineté éprouve le chemin de modèle de GalSen IA et **ne voit pas** un second runtime qui parlerait à un fournisseur hébergé sans passer par `ModelRouter`.
- **Le seul canal est `ModelSpec.api_key_env`**, lu dans `os.environ` par les trois adaptateurs — et **aucun modèle joignable en mode souverain n'en déclare**, les trois clés hébergées étant renseignées. Mesuré : `metadata` vaut `None` sur les trois modèles locaux, `openai_compatible` en rend zéro.
- **La protection n'est pas dans l'adaptateur, et le fichier le dit** : un `ModelSpec` fabriqué à la main nommant `OPENAI_API_KEY` est transmis tel quel par aider et par SWE-agent. La garde est la sélection, pas la traduction — laisser croire à une seconde barrière aurait été pire que le trou.
- **`sanitized_environment` filtre l'héritage, pas l'injection** : contre-test posé, sinon la liste blanche passerait pour une frontière qu'elle n'est pas.
- **Garde de couverture** : l'ensemble des moteurs déclarés doit égaler l'ensemble couvert ici, donc le quatrième back-end d'ADR-035 ne peut pas entrer sans être examiné.
- **Un test existe pour faire mordre la garde** : un modèle joignable se met à déclarer une clé, et la lecture doit le voir. Un garde qu'on n'a jamais vu refuser ne garde rien.
- Vérifié : suite complète **6 968 passent, 12 ignorés, 0 échec**, `ruff check .` propre. Le zéro échec tient à l'étiquette `v0.1.0` créée localement — **rien n'a été réparé**, et la CI échouera à l'identique tant qu'elle n'est pas poussée.

### 2026-08-20 (ADR-036 — la plateforme a enfin une licence : Apache-2.0)
- **Ce dépôt n'avait aucun fichier de licence** : `ls LICENSE*` ne rendait rien, et `pyproject.toml` ne déclare aucune métadonnée `[project]`. Le droit d'auteur par défaut s'appliquait — **personne ne pouvait légalement réutiliser ni redistribuer ce travail**. Relevé deux fois, par ADR-034 et ADR-035, juste après avoir refusé le manifeste d'un autre projet au motif qu'*un manifeste déclare, un fichier accorde*. La règle était appliquée vers l'extérieur et pas vers l'intérieur.
- **Mesuré avant de choisir** : les 19 dépendances d'exécution de `requirements.txt` lues — douze depuis les distributions installées, sept depuis les métadonnées publiées des paquets, non installés ici. **Zéro copyleft** : 8 MIT, 6 Apache-2.0, 4 BSD-3-Clause, 1 permissive composite. Les jeux optionnel, audio, embeddings, entraînement et développement **n'ont pas été lus**, et l'ADR le dit.
- **Apache-2.0 plutôt que MIT, pour la concession de brevet.** MIT est muet sur les brevets : un contributeur peut accorder le droit d'auteur en gardant un brevet qui lit sur ce qu'il a contribué. Routage, récupération, orchestration et méthodes d'agents sont du terrain breveté. Le §3 concède et révoque pour qui attaque ; le §5 définit ce qu'est une contribution ; le §4 donne une place à l'attribution (`NOTICE`).
- **Pas d'AGPL-3.0** : la plateforme livre une image et une API publique destinées à être déployées par des ministères, des universités, des ONG. Le copyleft la défendrait contre un risque qu'elle ne court pas encore, au prix de l'adoption pour laquelle elle existe.
- **Texte récupéré depuis `apache.org`, jamais reproduit de mémoire.** `LICENSE` et `NOTICE` nomment **« GalSen IA »** comme titulaire ; y mettre un nom légal ou une entité enregistrée est une modification d'une ligne, et elle appartient au propriétaire.
- **Conséquence négative, énoncée** : Apache-2.0 est **irrévocable pour les versions publiées sous elle**. N'importe qui peut bâtir un produit commercial dessus sans rien rendre. C'est le coût accepté de l'objectif d'adoption.
- Vérifié : `tests/test_published_numbers.py` a attrapé le compteur d'ADR resté à 36 — corrigé à 37. Suite complète et `ruff check .` relancés.

### 2026-08-20 (VOLET OPEN-SOURCE ECOSYSTEM AUDIT — 22 phases sur 22, ADR-037)
- **Audit de 12 projets open-source** (Transformers, SGLang, llama.cpp, LangGraph, OpenHands, vLLM, LiteLLM, LlamaIndex, Qdrant, Open WebUI, Unsloth, whisper.cpp). 14 documents → `docs/oss-ecosystem/`. **Zéro ligne de `src/`, zéro dépendance, zéro test touché** — le §12 interdit d'implémenter pendant l'audit.
- **Zéro `INTEGRATE` sur douze** : 2 déjà présents, 2 atteignables par un seuil existant, 4 différés, 2 conservés, 2 rejetés. Matrice de duplication : 2 `NO OVERLAP`, 4 `PARTIAL`, 4 `HIGH`, 2 `DIRECT DUPLICATE`.
- **La mesure du programme** : `SQLiteVectorStore.search()` relit et reparse chaque ligne à chaque requête. À 100 000 vecteurs, **13 132 ms** contre **3,88 ms** avec la matrice cachée — **3 388 ×**, pour 153,6 Mo résidents. La prémisse d'ADR-015, *« une matrice en mémoire »*, n'existe pas dans le code. **Ce n'a jamais été un problème de base vectorielle** : on s'apprêtait à accuser Qdrant d'un défaut de cache.
- **Le §4F n'est pas tenu** : `POST /coding/task` est gardé par `tool:execute`, que **`Role.USER` porte** ; `resolve_workspace()` accepte n'importe quel dossier de l'hôte ; `allow_network`/`allow_push` viennent du corps. **Exposition latente** — aucun des trois moteurs n'est disponible. Non corrigé, consigné.
- **Deux licences ne sont pas ce que leur manifeste annonce**, trouvées en lisant le fichier : LiteLLM réserve un `enterprise/` dont la licence répond **404** (PyPI déclare MIT simple, soit la lecture *plus permissive*) ; Open WebUI est BSD-3 **plus une clause 4** interdisant de retirer sa marque au-delà de 50 utilisateurs sur 30 jours.
- **Troisième audit externe d'affilée qui trouve un défaut ici plutôt que chez son sujet** — après le trou de souveraineté (ADR-034) et le `LICENSE` absent (ADR-035).
- **Programme terminé** : 16 documents, **ADR-037**, rapport → `docs/oss-ecosystem/final-report.md`. Un passage de relais avait été écrit à mi-parcours (`handover.md`) ; les 8 phases restantes ont été exécutées ensuite.
- Vérifié à chaque phase : `ruff check .` propre, **1 failed, 6967 passed, 12 skipped** (l'étiquette `v0.1.0`, identique sur `main`).

### 2026-08-20 (Constat n°1 de l'audit OSS corrigé : la recherche vectorielle)
- **`SQLiteVectorStore.search()` reconstruisait la matrice à chaque requête** — il relisait la table et reparsait chaque `values_json`. Ce que la condition de renversement d'ADR-015 mesurait n'était donc pas le coût du cosinus mais celui de l'analyse JSON.
- **Mesuré ici avant/après**, médianes sur 15 requêtes : **49,4 → 0,463 ms** à 271 vecteurs, **1 856,8 → 0,830 ms** à 10 000. L'audit avait mesuré 70,42 ms et 1 232 ms sur le même code intact ; deux exécutions sur un hôte partagé varient d'un facteur deux, et les deux chiffres sont dits plutôt que moyennés.
- **Le cache est validé par un compteur de version écrit dans la transaction de chaque écriture**, pas par un drapeau en mémoire : un cache que seul son processus sait invalider ment dès qu'un autre écrit. `PRAGMA data_version` aurait été exact aussi, mais **mesuré ici** une connexion neuve rend toujours `1`, et ce magasin en ouvre une par opération.
- **Les 5 gardes anti-péremption ont été vérifiées en sabotant la validation** : toutes passent au rouge. L'une ne discriminait pas — elle vérifiait le classement, identique dans les deux cas — et a été refaite pour vérifier le **score** périmé.
- **Deux nuances portées à ADR-015** : 94,93 ms est **sous** le seuil de 100 ms qu'ADR-015 se donne, pas au-delà ; et la correction **ne déplace pas** le déclencheur, elle retire un surcoût qui n'en faisait jamais partie.
- **Piège d'environnement mesuré au passage** : `bcrypt==5.0.0` est déclaré dans `requirements.txt` et n'était pas installé dans ce conteneur — **40 échecs et 16 erreurs**, tous d'authentification. La base **intacte** a été mesurée sur la même machine avant de conclure : les 40 échecs préexistaient, et la correction n'en introduit aucun (+11 tests, 0 échec).
- Suite complète : **6982 passent**, 9 ignorés, 3 désélectionnés, **0 échec**. `ruff` propre.

### 2026-08-21/22 (Constats n°2, 3 et 4 de l'audit OSS corrigés — les trois portillons trop étroits)
- **Constat n°2a — `/coding/*` n'était gardé que par `tool:execute`** (`src/coding_engine/authorization.py`). Or `Role.USER` la détient : c'est la permission qui ouvre la calculatrice. Le moteur déclare maintenant sa capacité (`data_scope: system`, `read+write`, `unattended: False`) et les deux routes consultent le **plafond de rôle existant** (`src/tool/authorization.py`) — aucune permission nouvelle : un second modèle d'autorisation pour la même question dérive du premier.
- Le refus est placé **avant** la validation du corps : un 404 sur un dossier absent aurait énuméré l'hôte pour un appelant non autorisé.
- **Constat n°2b — le confinement était relatif** (`src/coding_engine/workspace.py`). `confine()` empêche de sortir de l'espace de travail ; rien n'empêchait de choisir `~/.ssh` **comme** espace de travail, et l'en-tête du module promettait pourtant de le protéger. `GALSEN_CODING_WORKSPACE_ROOTS` déclare où un espace peut vivre, et **une variable absente refuse tout** au lieu de valoir « tout l'hôte ». Chemin résolu avant comparaison ; comparaison sur les composants, pas les préfixes — `/data/projet` ne contient pas `/data/projet-secret`.
- **Constat n°3 — l'approbation portait sur l'acte, jamais sur le contenu** (`src/training/feedback.py`). `export_pairs("oui")` passait : l'identifiant n'était jamais vérifié. Et même une vraie approbation disait « exporter le jeu » — or il grossit chaque jour, donc douze paires approuvées en autorisaient douze mille un mois plus tard. `request_export_approval()` inscrit une empreinte SHA-256 du texte exact ; `export_pairs()` la recalcule et refuse en nommant ce qui a changé. `scripts/training/train_adapter.py` a reçu `--empreinte`, **réutilisant la même fonction** — une seconde implémentation aurait dérivé.
- **Deux tests existants épinglaient le défaut** (`export_pairs(approval_request_id="req")` rendant des paires). Refaits pour passer par une vraie approbation — pas affaiblis : ils vérifient toujours ce qui sort et ce qui ne sort pas.
- **Constat n°4 — l'isolation des dépendances n'avait aucune garde.** `litellm==1.81.10` mesuré installé dans un environnement de plateforme, **déclaré par rien et importé par rien** : c'est l'arbre de dépendances d'un moteur qui a fui: exactement ce que `scripts/install_coding_engines.sh` existe pour empêcher, et ce qui avait déjà coûté une rétrogradation de numpy et un `import cv2` cassé sur toute la plateforme. Trois tests appliquent maintenant ce qui n'était qu'une consigne. Sur ce conteneur `litellm` est **absent**, numpy 2.4.6, cv2 5.0.0.
- **Chaque garde vérifiée en sabotant ce qu'elle garde** — y compris un sabotage d'abord fautif (ligne collée à la précédente, le fichier n'ayant pas de saut de ligne final) : le test était juste, la sabotage ne l'était pas.
- Suite complète : **7027 passent**, 9 ignorés, 3 désélectionnés, **0 échec**. `ruff check scripts src tests` propre. ADR-028 amendé trois fois, `docs/architecture/training-infrastructure.md` et `coding-engine.md` mis à jour, `.env.example` complété.
- **PR #34 fusionnée le 2026-08-22**, `main` à `5ce708a`. Mesuré sur `main` après la fusion (run 32555510451) : **`1 failed, 7020 passed, 15 skipped, 3 deselected`** — contre `1 failed, 6964 passed, 15 skipped` à `c88b555` avant. **+56 verts, le même unique échec** (l'étiquette `v0.1.0`). La CI de la PR a été comparée à celle de `main` **avant** de conclure, pas déduite du corps de la PR.
- **Un geste d'exploitant en découle** : `GALSEN_CODING_WORKSPACE_ROOTS` doit être renseignée au déploiement, sinon le moteur de codage refuse tout. Ajouté à `pending-work.md`, à côté du déploiement.

### 2026-08-22 (VOLET SUPERPOWERS COMPATIBILITY & INTEGRATION AUDIT — 24 phases, `PARTIAL-GO`)
- **`obra/superpowers` audité à `b36e0829` (v6.3.0)**, cloné par le proxy git après que `api.github.com` et `github.com` ont répondu **403** — `raw` seul aurait imposé `UNKNOWN` pour la version et de **deviner** quels fichiers existent. Rapport : `docs/research/superpowers-audit.md`, 1 836 lignes, les 18 points de §25 plus le bloc §26.
- **Ce que c'est, mesuré** : **29 322 lignes de prose contre 4 012 de code**, MIT, **zéro dépendance déclarée**, aucun lockfile, aucun code tiers. Ni modèle, ni runtime, ni **bibliothèque** : il n'y a **aucune surface d'import**. « Intégrer » n'a jamais été une question technique, seulement éditoriale.
- **37 sous-systèmes comparés : 19 `KEEP GALSEN`, 10 `ADAPT`, 3 rien, 1 `IMPORT`, `REPLACE` = 0.**
- **Le constat qui porte la décision** : les 15 fichiers de règles, les 14 skills et `CLAUDE.md` n'ont **aucune preuve** de changer le comportement d'un agent. Ce dépôt exige de saboter une garde avant d'y croire — et ne l'a jamais appliqué à la prose qui le gouverne. Superpowers fournit la méthode manquante (RED-GREEN-REFACTOR sur de la documentation de processus, avec scénarios de pression).
- **Un conflit direct, tranché** : « ne pas s'arrêter entre les tâches » contre `phase-protocol.md`, permanent. Exclu nommément de tous les candidats — c'est la raison pour laquelle la recommandation est l'adoption native et non l'installation.
- **Un constat de sécurité** : le bootstrap injecte des instructions à chaque session et se met à jour « souvent automatiquement ». Cela **inverse** la règle de `trust.py` (un texte externe est une donnée, jamais une instruction). Vise **le mécanisme de distribution**, jamais la prose lue à un commit épinglé.
- **Télémétrie vérifiée dans le code, pas dans le README** : une `<img>`, la version seule, `no-referrer`, trois opt-outs, quatre tests. Noté quand même : un GET révèle IP, user-agent et horodatage — §16 interdit de le dire anodin.
- **Une affirmation de la phase 3.2 corrigée en cours d'audit** : GalSen IA **a** un relecteur (`coder → reviewer`). Ce qui manque est la **boucle** (rondes bornées, re-revue ciblée, arbitrage). Corrigé dans le document plutôt qu'en silence.
- **Coût de la voie recommandée : zéro dépendance, ~0 octet ajouté au démarrage** (les règles se chargent à la demande). Mesuré au passage : GalSen IA injecte déjà **14 169 octets** par session, 4,5× la charge auditée.
- **5 `UNKNOWN` laissés debout**, dont le coût-modèle de la boucle de sous-agents et le coût par test de comportement — les deux exigent qu'un modèle réponde, et C1 (`ollama serve`) est toujours ouvert.
- Vérifié à chaque phase (`test_published_numbers` + `test_package_documentation`, 10/10) et à la clôture : **7027 passed, 9 skipped, 0 failed**, `ruff` propre. **Zéro fichier hors `docs/`.**

### 2026-08-22 (VOLET SUPERPOWERS — audit 24 phases puis implémentation 11 phases, ADR-038)
- **`obra/superpowers` à `b36e0829` (v6.3.0, MIT) audité** → `docs/research/superpowers-audit.md`, 1 836 lignes, décision **`PARTIAL-GO`**. Puis **6 candidats implémentés** en 11 phases.
- **Ce que c'est, mesuré** : **29 322 lignes de prose contre 4 012 de code**, zéro dépendance, **aucune surface d'import**. « Intégrer » n'a jamais été une question technique, seulement éditoriale. **19 sous-systèmes sur 37 : `KEEP GALSEN`. `REPLACE` : zéro.**
- **Le constat qui a décidé l'ADR porte sur GalSen IA, pas sur Superpowers** : 15 règles, 15 skills et un `CLAUDE.md` sans **aucune preuve** de changer le comportement d'un agent. Ce dépôt refuse de croire une garde tant qu'elle n'a pas été sabotée — et ne l'avait jamais appliqué à sa propre prose.
- **Ce qui a atterri** : C1 `skills/testing-instructions/`, C2 `skills/systematic-debugging/`, C3 la clause de fraîcheur (`verification.md`), C4 le format de décision (`memory.md`, `phase-protocol.md`), C5 la fin d'une branche (`git-workflow.md`), C6 `scripts/find_polluter.py` — **seule vraie copie, notice MIT et commit d'origine dans l'en-tête**.
- **Rien n'est installé**, et c'est la moitié porteuse : le plugin importerait une cadence contredisant le protocole de phases, un flux d'instructions auto-mis-à-jour qui **inverse** la règle de `trust.py`, et 9 skills inutiles. Quatre exclusions de plus sont nommées dans ADR-038 pour qu'elles ne dérivent pas.
- **C3 mesurée par C1 — première règle de l'histoire de ce dépôt éprouvée au lieu d'être crue.** ROUGE sans la clause (l'agent ouvre par « Oui » et cite `work-cadence` pour se justifier), VERT avec. Décision : *adopter le format de décision sans la cadence qui l'accompagne — parce que la cadence contredit une règle permanente — coûte de perdre la fluidité que la source revendique, et c'est le prix accepté.*
- **La campagne a trouvé deux choses qu'aucune lecture n'aurait produites** : C3 **créait** un conflit avec `work-cadence.md` (fermé le jour même — c'était mon changement, donc à moi de le fermer) ; et **les sous-agents héritent de `CLAUDE.md`**, donc une ligne de base sans la règle n'est **pas atteignable par instruction**. Le skill consigne la revendication plus faible qu'il peut réellement soutenir.
- **L'étape de preuve de C6 a trouvé un vrai défaut dans mon portage au premier essai** (`TypeError` dans la dichotomie). L'adopter sans cette étape aurait adopté un outil cassé. Attrapé aussi : un code de sortie lu à travers un `| tail` rapportait le statut de `tail`.
- **Coût mesuré de C1** : ~59 000 tokens et 7–11 s par dépêche, ~60 000 par bras. `n = 1 par bras`, et la campagne le dit.
- Vérifié à chaque phase : **7027 passed, 9 skipped, 0 failed**, `ruff` propre, hook de session exit 0. **Zéro ligne de `src/`, zéro dépendance, zéro test modifié ou supprimé.**

### 2026-08-22 (AUDIT #01 `codebase-memory-mcp` — 16 phases, `KEEP FOR RESEARCH`)
- **`DeusData/codebase-memory-mcp` à `010569fa` (MIT)** audité → `docs/research/codebase-memory-mcp-audit.md`. **Rien installé, rien intégré, rien adapté ; zéro fichier hors `docs/`.**
- **Ce que c'est** : **projet en C de 1,3 Go** (842 `.c`, dont 1,2 Go de 160 grammaires tree-sitter), graphe de propriétés SQLite, **14 types de nœuds et 8 relations**, incrémental par hachage de contenu, **30 Mo de poids `nomic-embed-code` embarqués**.
- **Le recadrage qui décide** : GalSen IA a **`code-review-graph` déjà branché**. **16 lignes sur 24 sont `KEEP`** — non parce que le Python de GalSen IA couvre, mais parce que CRG couvre. Ce n'est pas une capacité manquante, c'est **un second fournisseur d'une capacité existante**, et l'adopter ferait tourner **deux graphes sur un seul arbre**.
- **Décision** : `KEEP FOR RESEARCH`, avec un déclencheur nommé vers l'usage en développement. *Décision : ne pas adopter aujourd'hui — parce que les deux portes qui le justifieraient (performance, gain de contexte) sont `UNKNOWN` et que la mesure décisive ne pouvait pas tourner ici — coûte de retarder un gain réel si la mesure lui donne raison, et ce retard est le prix accepté pour ne pas choisir sur une revendication.*
- **Le risque, assumé par le projet lui-même** : il écrit des **fichiers d'instructions** dans `$HOME` (`global_rules.md`, `AGENTS.md`). **Même classe de surface qu'ADR-038 a écartée pour Superpowers.** Et **le retrait n'est pas propre** : désinstaller ne les efface pas.
- **Sa revendication « 100 % local » a survécu à une vérification dans le C** : aucun `curl`/`SSL_connect`/`socket(AF_INET)` dans `src/` ni `internal/cbm/` ; la seule socket `AF_INET` vise `htonl(0x7F000001)`.
- **Deux faux positifs écartés en lisant les correspondances plutôt que les comptes** : « 7 MPL » était `si**mpl**ecpp` et « i**mpl**ementation » — il n'y a aucune MPL ; et `getaddrinfo` était une **table générée de noms de la stdlib Python**, pas un appel. Le premier aurait été le constat de licence le plus important de l'audit, et il était faux.
- **Performance : `NOT_MEASURED`.** Leurs chiffres sont consignés avec leur méthode (M3 Pro) mais leur ligne de base est `grep`, choisie par l'auteur ; la seule qui compte ici est CRG. Le banc est **défini et non exécuté** — son bras décisif exige les outils MCP de CRG, déconnecté deux fois pendant la session.
- Vérifié : **7027 passed, 9 skipped, 0 failed**, `ruff` propre. Aucun test ajouté, aucun code écrit.

### 2026-08-24 (Couche modèle — le routage sélectionnait le premier de la liste, ADR-040)
- **Mesuré avant de toucher quoi que ce soit**, sur un parc local de cinq modèles spécialisés : `code_generation`, `reasoning` et `conversation` retournaient **le même modèle — le premier de la liste, trois fois** ; `vision`, `summarization` et `document_analysis` ne retournaient **rien**. La couche de sélection par capacité existait, était branchée, et comparait des descripteurs **identiques pour tous les modèles**.
- **Cinq causes distinctes, toutes mesurées** : (1) `LocalProvider` construisait un descripteur unique — pas de vision, pas d'outils, 8192 jetons, et trois atouts hors du vocabulaire de routage ; (2) `/api/tags` **ne porte pas** de `context_length`, la clé lue n'existait pas, donc tout modèle valait 8192 et les tâches à long contexte étaient irroutables par construction ; (3) une complexité **non annoncée** était traitée comme `medium`, imposant un plancher de 8192 jetons — celui qui écartait le seul modèle de vision ; (4) `_pick_best` rendait le premier candidat portant **un** atout, et `prefer_cheapest` court-circuitait avant les atouts, or des modèles locaux tous gratuits sont une égalité générale tranchée par l'ordre de la liste ; (5) **`generate_text_with_fallback` n'appelait jamais le sélecteur** — tout le travail de routage vivait dans `ProviderSelector`, que le chemin de génération du chat ne consultait pas.
- **Et une sixième, à la couture entre sous-systèmes** : **sept des huit intentions du planner n'avaient aucune règle de routage**. Le chat transmet cet axe tel quel, donc « écris une fonction Python » arrivait en `implementation`, tombait sur `default` (`general_conversation` + `prefer_cheapest`) et se faisait écrire par le plus petit modèle installé.
- **Nouveau** : `src/model_engine/local_catalogue.py` — trois origines classées, **`measured` > `declared` > `default`**, et `ModelDescriptor.capability_sources` dit laquelle a fixé chaque champ. La mesure vient de `POST /api/show` d'Ollama (`capabilities`, `model_info.<arch>.context_length`), **forme vérifiée sur la documentation officielle** (`ollama/ollama`, `docs/api.md`, récupérée).
- *Décision : `None` n'est pas `False` pour une capacité — une vision non constatée n'est pas une absence de vision — parce qu'une mesure muette ne doit pas effacer ce que la configuration déclarait ; coûte, si c'est faux, qu'un serveur ancien laisse une déclaration erronée survivre à une mesure qui l'aurait corrigée.*
- *Décision : ajouter les intentions du planner à la politique de routage plutôt que les renommer dans le planner — parce qu'elles désignent aussi les agents à mobiliser — coûte un fichier de configuration plus long et un test qui doit surveiller la dérive entre les deux listes (`test_aucune_intention_du_planner_n_est_orpheline`).*
- **Résultat mesuré** : dix types de tâche et huit intentions atteignent **cinq modèles distincts**. `tests/test_local_model_profiles.py` (**47 tests**) épingle la table ; retirer l'intégration en fait tomber huit — **vérifié par sabotage**.
- **Recherche modèles** → `docs/models/local-model-selection.md`. **Aucun modèle téléchargé, chargé ni évalué** ; aucune revendication de qualité ne dépasse `OBSERVED`. `huggingface.co` et `qwenlm.github.io` sont **refusés par le mandataire** (`EGRESS_BLOCKED`, mesuré) : les fiches de modèles sont illisibles d'ici.

### 2026-08-24 (P3 — le chat critique sa propre réponse, ADR-041 ; `src/skills/` branchée)
- **Le défaut** : ADR-039 avait donné au chat une étape de rédaction qui génère **une fois**. Rien ne relisait. Une réponse contenant « le total est 2 + 2 = 5 », affirmant « il est prouvé que… » sur ce que la plateforme n'a jamais vérifié, ou contredite par un constat qu'elle a elle-même rassemblé, était servie telle quelle.
- **Nouveau** : `src/reasoning/` — `critics.py` **constate** et ne corrige rien ; `deliberation.py` décide quoi faire d'un constat et quand cesser. La frontière est la conception : `agents/verifier/agent.py` posait déjà cette règle pour la vérification factuelle.
- **Aucun critique n'interroge un modèle.** Cinq contrôles déterministes, quatre bloquants : réponse vide, calcul faux (en `Decimal`), affirmation contredite par un constat, certitude affichée sans ancrage. Nommer les rouages internes est un **signal**, pas un blocage — relancer une génération pour un mot mal choisi coûte plus cher que le défaut.
- **Trois arrêts, et le rapport dit lequel a servi** : `verified`, `iteration_budget_exhausted`, `deadline_exceeded`. **Budget épuisé ⇒ la réponse est servie AVEC ses constats.** Une boucle qui rend en silence une réponse qu'elle sait douteuse vaut moins que pas de boucle : elle ajoute une garantie qui n'existe pas. Une reprise par défaut, `GALSEN_CHAT_MAX_RETRIES` ; `0` garde la critique et n'arrête que la reprise.
- **Banc mesuré** — `python -m src.reasoning.benchmark`, 22 cas : **66,7 % de détection, 0 % de fausse alerte**. *Décision : ajouter au banc quatre cas que les contrôles ratent vraiment (calcul en toutes lettres, sans signe égal, pourcentage, contradiction reformulée) — parce que sa première version se donnait 8/8, cas et contrôles sortant de la même main — coûte un score qui a l'air moins bon et qui, lui, veut dire quelque chose.*
- **Défaut trouvé en construisant** : le premier contrôle `empty_answer` exigeait trois mots et signalait « 42 », « Oui. », « Non, à Dakar. » — des réponses complètes. Un vérificateur qui pénalise la concision coûte un appel de modèle et n'améliore rien. **Vide veut dire vide.**
- **`src/skills/` est branchée** (`src/skills/loop.py`). Elle existait depuis le 2026-08-23 et **rien n'y écrivait**. Le `coder` **retrouve** avant d'écrire (antériorité, jamais réponse toute faite) ; le `tester` **range**, et seulement si des suites ont réellement tourné et sont vertes. *Décision : ranger côté `tester` et non côté `coder` — parce que c'est le seul endroit du dépôt où existe une preuve, et que `Competence.valider()` refuse une compétence qui se dit vérifiée sans dire par quoi — coûte que rien n'est rangé quand aucun test ne tourne, ce qui est le cas ordinaire.*
- **Un verdict vert sans exécution ne prouve rien** : le `tester` rend `passed: True` quand il s'exclut par ré-entrance ; ce cas est refusé explicitement.
- Vérifié : **7309 passés, 9 ignorés, 3 désélectionnés, 0 échec**, `ruff` propre. **62 tests ajoutés, 0 supprimé, 0 affaibli.** Deux gardes du dépôt ont attrapé de vrais oublis : `GALSEN_CHAT_MAX_RETRIES` non documentée dans `.env.example`, et le nombre d'ADR annoncé dans `CLAUDE.md`.

### 2026-08-24 (Phase 3 — la préférence tranche entre égaux, un grand modèle est une URL, ADR-042)
- **Le défaut mesuré** : après ADR-040, une tâche `reasoning` trouvait **trois** modèles portant l'atout `reasoning` (`qwen3.5:9b`, `qwen2.5:14b`, `deepseek-r1:8b`). Tous locaux, donc tous gratuits : le coût ne tranchait rien et le choix retombait sur **l'ordre d'installation**.
- **`role_preferences`** dans `config/model_routing.yaml` : par rôle, des motifs ordonnés, consultés **uniquement entre candidats déjà également capables**. *Décision : la préférence ne remonte jamais un modèle moins capable — parce qu'une préférence qui primerait la compétence serait un routage en dur, ce que ce fichier existe pour éviter — coûte qu'un rôle mal ordonné envoie la tâche à un modèle légèrement moins bon parmi des égaux, le repli atteignant toujours les autres.* Onze rôles atteignent cinq modèles distincts.
- **`qwen3.5:9b` reconnu**, 262 144 jetons — le premier modèle local à franchir le seuil de `document_analysis` (100 000). Son motif est placé **avant** la ligne généraliste, qui contient « qwen3 » et l'avalerait. *Décision : ne PAS déclarer sa vision — annoncée par des sources secondaires, elle sera mesurée par `/api/show` — coûte que la vision reste sur `llava` tant qu'aucun serveur ne tourne, au lieu d'envoyer des images à un modèle qui ne les lit peut-être pas.* `qwen2.5:14b` conservé comme ligne de base.
- **`config/models/`** : quatre familles serveur préparées (Kimi K2.5, Qwen3.5-397B-A17B, DeepSeek-R1-0528, GLM-5.1). **Chaque `serve_command` est recopiée des recettes officielles de vLLM** (`vllm-project/recipes`, lues via `raw.githubusercontent.com` le 2026-08-24), jamais reconstruite de mémoire.
- **Aucun code n'a été nécessaire pour atteindre un serveur** : `OpenAICompatibleProvider` parle déjà le contrat que servent vLLM et SGLang. Un grand modèle est une URL et un nom.
- *Décision : le contrôle de santé interroge `/v1/models` et non `/health` — parce que le chemin de santé de vLLM n'a pas pu être vérifié (`docs.vllm.ai` refusé par le mandataire) — coûte de dépendre du contrat OpenAI plutôt que d'un point dédié, ce qui est précisément le chemin qu'emprunte déjà la plateforme.*
- **Quatre scripts, tous exécutables** : `scripts/models/preflight.py`, `serve_large.py`, `connect.py`, `bench.py`.
- **Aucun modèle téléchargé, chargé ni évalué.** Mesuré le 2026-08-24 : aucun GPU (`nvidia-smi` absent), pas d'Ollama, et **tous les hôtes de poids refusés par le mandataire** — `registry.ollama.ai`, `ollama.com`, `huggingface.co`, `cdn-lfs.huggingface.co` rendent `000`. Ce qui **a** été éprouvé, ce sont les refus : `bench.py` ne rend **aucun chiffre** plutôt qu'un zéro, et la comparaison refuse d'accoler un passage `SCRIPTED` à un passage `REAL`.
- *Décision : un écart de moins d'une tâche et demie est rapporté `ÉGALITÉ` et non une victoire — parce que c'est le garde-fou contre « le plus récent est meilleur » — coûte de ne pas départager deux modèles réellement proches, ce qui est l'aveu correct.*
- **L'infrastructure d'entraînement n'a pas été refaite** : `scripts/training/train_adapter.py` est déjà une recette QLoRA réelle avec registre de lignée. En ajouter une seconde pour paraître productif dupliquerait de l'existant qui marche.
- Vérifié : **7345 passés, 9 ignorés, 3 désélectionnés, 0 échec**, `ruff` propre. **36 tests ajoutés, 0 supprimé, 0 affaibli.**

### 2026-08-24 (Les dix épreuves, câblées sur le vrai `/chat` — et le blocage exact)
- **La demande** : les premières réponses réelles de GalSen IA, par un vrai modèle. **Elles n'existent pas**, et voici pourquoi, mesuré plutôt qu'affirmé.
- **Le moteur d'inférence s'installe ; les poids sont inatteignables.** `llama-cpp-python 0.3.35` installé depuis pypi et importable — un vrai moteur est présent. Mais **tous les hôtes de poids sont refusés par la passerelle** : `registry.ollama.ai`, `ollama.com`, `huggingface.co`, `hf-mirror.com`, `modelscope.cn`, `gpt4all.io` → `000` ; `github.com/…/releases` → `403`. La liste `noProxy` du mandataire nomme ce qui passe — npm, jsr, pypi, crates.io, proxy.golang.org — et **aucun paquet pypi n'embarque de poids utilisables** (`tinyllama`, `smollm`, `llm-gguf`, `minillm` : code seul, ≤ 30 Ko).
- **Nouveau** : `src/model_engine/evaluation_suite.py` — les dix épreuves du propriétaire, passées par **`POST /chat`** et non par un fournisseur direct. Elles traversent planner, agents, ancrage, rédaction, critique déterministe et reprise. Elles mesurent ce qu'un utilisateur reçoit, ce qui compte pour six d'entre elles : un calcul faux est jugé **après** la boucle de délibération.
- *Décision : trois issues et non deux — `PASS`, `FAIL`, `NOT_CHECKED` — parce que quatre épreuves (expliquer l'IA, une stratégie de chantier, une PME, du wolof) n'ont aucune vérité vérifiable par machine ; coûte de ne pouvoir noter que six épreuves sur dix, l'alternative étant d'afficher dix résultats dont quatre inventés.*
- *Décision : une réponse **non générée** n'est jamais notée — parce que la plateforme compose parfois un repli à partir de ce que les agents ont rapporté — coûte que rien n'est mesurable tant qu'aucun modèle ne répond, ce qui est exactement l'état actuel.*
- **`ChatResponse` porte maintenant `model_used` et `deliberation`** : des valeurs déjà calculées, rendues visibles à un appelant de l'API.
- **La clé d'API jetable n'est pas un contournement** : elle est provisionnée par le mécanisme documenté (`GALSEN_API_KEYS`), l'authentification vérifie exactement ce qu'elle vérifiait, et la clé n'est ni écrite ni affichée.
- **Résultat mesuré** : les dix épreuves tournent, traversent toute la chaîne (≈ 1 s d'orchestration chacune), et rendent toutes `NOT_EXECUTED — aucun fournisseur de modèle n'est disponible`. **Aucun chiffre n'est rendu.**
- Vérifié : **7371 passés, 9 ignorés, 3 désélectionnés, 0 échec**, `ruff` propre. **26 tests ajoutés, 0 supprimé, 0 affaibli.**

### 2026-08-24 (Premier passage réel des dix épreuves, et le défaut qu'il a révélé)
- **GalSen IA a produit ses premières vraies réponses.** Machine du propriétaire, RTX A2000 12 Go, `qwen3.5:9b` + `qwen2.5-coder:14b` via Ollama. **10 épreuves sur 10 exécutées**, 1700 s au total. **4 réussites sur 6 vérifiables** ; 4 épreuves `NOT_CHECKED` par construction.
- **Le préflight a validé la conception d'ADR-040 sur le terrain** : `contexte 262144 (mesuré)` et surtout **`vision True (mesuré)`**. J'avais refusé de déclarer la vision de Qwen3.5 depuis un résumé de recherche ; `/api/show` l'a constatée. La règle « ne pas déclarer ce qu'on n'a pas mesuré » a produit le bon résultat sans que personne ait à deviner.
- **Défaut trouvé et corrigé** : `LocalProvider.GENERATION_TIMEOUT_SECONDS` valait 120 s. `qwen3.5:9b` l'a dépassé **deux fois sur dix**. *Décision : porter le défaut à 300 s et le rendre réglable par `GALSEN_LOCAL_GENERATION_TIMEOUT` — parce que le coût d'un délai trop court n'est pas la lenteur mais le **changement de modèle silencieux** : sur TEST-03 le repli a envoyé une question d'arithmétique au modèle de code, qui a mal répondu — coûte qu'un serveur réellement bloqué immobilise un tour plus longtemps.*
- **Un trou de ma propre critique est sorti dès la première utilisation réelle** : le contrôle arithmétique ne reconnaît que la forme `a op b = c`. TEST-03 s'est terminé sur `arrêt : verified` alors que la réponse ne portait pas le bon total. Je l'avais listé comme raté connu dans `src/reasoning/benchmark.py` (`calc-faux-sans-signe-egal`) ; le banc avait raison. **Correction en attente du contenu de `rapport.json`** — savoir si le modèle s'est trompé ou si je n'ai pas su lire sa réponse appelle deux correctifs différents.
- **Coût de l'orchestration, mesuré** : le planner prend 58 à 126 s par tour, soit environ la moitié du temps total. Il appelle le modèle pour analyser la question avant qu'on y réponde.
- Vérifié : **7378 passés, 9 ignorés, 3 désélectionnés, 0 échec**, `ruff check .` propre. **5 tests ajoutés, 0 supprimé, 0 affaibli.**
