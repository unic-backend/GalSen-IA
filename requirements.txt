# =============================================================================
# GalSen IA — Core Dependencies
# =============================================================================
# Install with: pip install -r requirements.txt
# For optional features: pip install -r requirements.txt --extra-index-url ...
# Full installation: pip install -r requirements-dev.txt

# === Core (toujours requis) ===
fastapi>=0.100.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
PyYAML>=6.0

# === Vision & Image Processing (PIL, OpenCV) ===
# Vision Intelligence Engine, OCR Tool
# Pillow est importé en lazy — son absence désactive les fonctionnalités vision/OCR
Pillow>=10.0.0
# opencv est importé au niveau module par le Vision Intelligence Engine
# (object_detector, contour_object_detector, quality_analyzer, image_classifier) :
# sans lui le moteur `vision` est indisponible dans l'EngineRegistry.
# Variante headless : aucune dépendance à un serveur graphique (le code n'affiche rien).
opencv-python-headless>=4.8.0

# === Machine Learning (numpy, scipy) ===
# Knowledge Engine (ranking), Vision Engine
numpy>=1.24.0

# === Testing ===
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
# Requis par starlette.testclient.TestClient (tests API : health, rate limiter, RBAC, auth).
# starlette attend httpx2 : avec l'ancien httpx il fonctionne mais avertit à chaque import.
httpx2>=2.9.0